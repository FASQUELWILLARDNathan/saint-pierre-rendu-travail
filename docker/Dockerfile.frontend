FROM node:22-alpine

WORKDIR /app

# install deps first
COPY package*.json ./
RUN npm ci

# app source
COPY . .

# avoid watching heavy folders
ENV CHOKIDAR_USEPOLLING=true

EXPOSE 5173

CMD ["npm", "run", "dev"]